services:
  - type: web
    name: deals-hunt
    env: python
    region: oregon
    plan: free
    buildCommand: ""
    startCommand: python server.py
    envVars:
      - key: PORT
        generateValue: false
