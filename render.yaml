services:
  - type: web
    name: deals-hunt
    runtime: python
    region: oregon
    plan: free
    buildCommand: pip install --quiet --upgrade pip
    startCommand: python server.py
    healthCheckPath: /api/status
